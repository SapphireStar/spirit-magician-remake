using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PbSpirit;
using System.IO;

public class GenerateElf
{
/*    static SpiritPackage Deserialize(string path)
    {
        SpiritPackage sp = new SpiritPackage();
        try
        {
            using (var input = File.OpenRead(path))
            {
                sp = SpiritPackage.Parser.ParseFrom(input);

            }
        }
        catch (System.Exception e)
        {

            Debug.LogError(e);
        }
        return sp;

    }*/
/*    public static List<Spirit> InitPackage(string path)
    {
        //SpiritPackage spPack = Deserialize(path);
        List<Spirit> sp = new List<Spirit>();
        foreach( var item in spPack.SpiritPackage_)
        {
            sp.Add(item);
        }
        return sp;
    }*/
   
}
